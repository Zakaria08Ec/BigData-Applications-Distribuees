version: '3.8'

services:
  web:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - .:/app
    environment:
      - PYTHONUNBUFFERED=1
    # Cette commande s'assure que le serveur démarre correctement
    command: uvicorn src.main:app --host 0.0.0.0 --port 8000 --reload